import BookCard from "@/components/module/BookCard";
import Header from "@/components/module/Header";

const Home = () => {
  return (
    <main>
      <Header/>
      <BookCard/>
    </main>
  );
};

export default Home;
